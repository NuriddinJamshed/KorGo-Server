{
    "name": "my-json-api",
    "version": "1.0.0",
    "scripts": {
        "start": "json-server --watch db.json --port $PORT"
    },
    "dependencies": {
        "json-server": "^0.17.4",
        "json-server-auth": "^2.1.0"
    }
}